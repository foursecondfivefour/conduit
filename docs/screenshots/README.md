# Screenshots

Place PNG captures here for the README:

- `splash.png` — splash screen
- `onboarding.png` — onboarding wizard
- `tray.png` — system tray menu

Capture on Windows after `go build -tags production -o build\conduit.exe .` and running the app.
